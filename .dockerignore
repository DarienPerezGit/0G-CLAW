# Dependencies
node_modules
.pnpm-store

# Build output
dist
*.tsbuildinfo

# Git
.git
.gitignore
.github

# Secrets / local env (.env.example stays so it's discoverable inside the image)
.env
.env.*
!.env.example

# Local agent state — mounted as volume in docker-compose, must not be baked in
data
.0g-claw

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode
.idea

# Tests / coverage
coverage
.nyc_output

# Docker meta — not needed inside the image
Dockerfile
docker-compose.yml
.dockerignore
