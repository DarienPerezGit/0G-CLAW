/**
 * basic-agent — stub
 * Full implementation in feat/basic-agent.
 * This file exists so that `pnpm build` succeeds before the agent is wired up.
 */
console.log('0G-Claw basic agent — not yet implemented. Run after feat/basic-agent is merged.');
process.exit(0);
